# Nixpacks configuration for Railway
# This file tells Railway how to build and run different services

[phases.setup]
nixPkgs = ["...", "python39"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
# Default start command (for web service)
cmd = "bash start.sh"
