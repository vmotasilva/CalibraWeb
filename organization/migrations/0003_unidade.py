# Generated by Antigravity on 2026-08-22

from django.db import migrations, models


def seed_unidades_iniciais(apps, schema_editor):
    Unidade = apps.get_model("organization", "Unidade")
    unidades_iniciais = [
        {"nome": "Tecnolens Laboratório Ótico Feira Ltda", "codigo": "FSA", "cidade": "Feira de Santana", "estado": "BA"},
        {"nome": "Tecnolens Matriz", "codigo": "MTZ", "cidade": "Feira de Santana", "estado": "BA"},
    ]
    for u in unidades_iniciais:
        Unidade.objects.get_or_create(nome=u["nome"], defaults=u)


class Migration(migrations.Migration):

    dependencies = [
        ("organization", "0002_initial"),
    ]

    operations = [
        migrations.CreateModel(
            name="Unidade",
            fields=[
                ("id", models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name="ID")),
                ("nome", models.CharField(max_length=150, unique=True, verbose_name="Nome da Unidade / Empresa")),
                ("codigo", models.CharField(blank=True, default="", max_length=50, verbose_name="Código / Sigla")),
                ("cnpj", models.CharField(blank=True, default="", max_length=20, verbose_name="CNPJ")),
                ("cidade", models.CharField(blank=True, default="", max_length=100, verbose_name="Cidade")),
                ("estado", models.CharField(blank=True, default="", max_length=2, verbose_name="UF")),
                ("ativo", models.BooleanField(default=True, verbose_name="Ativo")),
                ("criado_em", models.DateTimeField(auto_now_add=True)),
            ],
            options={
                "verbose_name": "Unidade / Empresa",
                "verbose_name_plural": "Unidades / Empresas",
                "ordering": ["nome"],
            },
        ),
        migrations.RunPython(seed_unidades_iniciais, migrations.RunPython.noop),
    ]
