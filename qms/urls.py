from django.urls import path

from . import views


urlpatterns = [
	# NOTE: All URL routes are now defined in config/urls.py to avoid conflicts
	# This file is kept for backward compatibility but is no longer actively used
]
